<section id="rpg-ui-buttons">
  {{#each buttons}}
    <button data-id="{{rt}}" class="button{{#if active}} active{{/if}}" title="{{localize name}}">
      <i class="{{icon}}"></i>
    </button>
  {{/each}}
</section>
